import { defineConfig } from 'vite';

export default defineConfig({
  // 注意：这里最好填您的仓库名称，前后加斜杠
  // 比如您的仓库名是 Calculation-of-Compressor-Efficiency-pro
 base: '/Calculation-of-Compressor-Efficiency-pro/',
  
  server: {
    open: true,
  }
});